# Program to sort alphabetically the sentences form a string provided by the user

my_str = "Hello this Is an Example With cased letters"

# To take input from the user
#my_str = input("Enter a string: ")

# breakdown the string into a list of sentences
sentences = [sentence.lower() for sentence in my_str.split()]

# sort the list
sentences.sort()

# display the sorted sentences

print("The sorted sentences are:")
for word in sentences:
   print(sentence)
